name: Build & Deploy
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Deploy project
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }} # IP address of the remote server
          key: ${{ secrets.SSH_KEY }} # Private or public key of the server
          username: ${{ secrets.SSH_USERNAME }} # User of the server
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Specify the Node.js version

      - name: Install Yarn
        run: npm install -g yarn # Install Yarn globally

      - name: Build
        run: yarn build # Build the project

      - name: Deploy
        run: |
          # Add your deployment script here
          # For example, SSH into a server and pull the latest changes
          ssh $username@$host 'cd /var/www/wordpress/roots && git pull'

      # - name: Deploy project
      #   uses: actions/checkout@v4
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: 18
      #   run: npm ci
      # - script: |
      #     ssh $username@$host 
      #     echo -e "update" >> example.txt
      #     cd /var/www/wordpress/roots
      #     git pull origin main
      #     cd /var/www/wordpress/roots/web/app/themes/sage/
      #     yarn build
      #     echo 'Deployment successful to remote server' 
